# render

Fingerprinted paper generation. Block-constrained question and option permutation, typographic watermarking, overt copy serials, and the per-copy answer-key mapping that scoring consumes. The most subtly difficult component in the system.

See `docs/10-feature-catalogue.md`.
