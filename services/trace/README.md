# trace

Attribution. Takes a leaked image, normalises it, recovers the permutation signature and watermark, and returns centre plus copy serial with a confidence score and a signed report suitable as an FIR annexure.

See `docs/10-feature-catalogue.md`.
