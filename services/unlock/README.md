# unlock

Package latch authorisation. Evaluates device attestation, roster membership, geofence, time window and package state, then issues a single-use signed unlock token. Deny-by-default; denied attempts are first-class ledger events.

See `docs/05-unlock-protocol.md`.
