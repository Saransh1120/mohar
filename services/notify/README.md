# notify

Rule engine over the ledger stream. Custody gaps, off-window seal events, geofence violations, fallback invocations, print-window overruns. Escalates by severity to control room, authority and observer.

See `docs/02-architecture.md`.
