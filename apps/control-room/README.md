# control-room

Operations dashboard. Live package map (Leaflet + OpenStreetMap, no billing), alarm queue, dual-authorisation approvals for the crypto fallback path, incident timeline, signed evidence-pack export.

See `docs/07-field-ops-runbook.md`.
