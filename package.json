{
  "name": "mohar",
  "private": true,
  "version": "0.0.1",
  "description": "Sealed examination paper custody chain",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "migrate": "node infra/migrations/run.mjs",
    "test:crypto": "pnpm --filter @mohar/crypto-core test"
  },
  "devDependencies": {
    "pg": "^8.13.0",
    "turbo": "^2.1.0",
    "typescript": "^5.6.0"
  },
  "engines": { "node": ">=22" }
}
